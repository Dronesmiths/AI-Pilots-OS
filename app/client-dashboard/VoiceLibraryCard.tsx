'use client';
export default function VoiceLibraryCard({ initialFavorites = [] }: { initialFavorites?: string[] }) {
  return <div style={{ padding: 20, color: '#80868b', textAlign: 'center' }}>Voice Library — Coming Soon</div>;
}
